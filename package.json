{
  "name": "sushi",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "heroku-postbuild":"npm run build2 && npm run install-client && npm run build"
  },
  "devDependencies": {
    "vite": "^5.0.8"

  },
  "dependencies": {
    "aos": "^2.3.4"
  }
}
